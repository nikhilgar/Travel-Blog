<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travel Website</title>
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            overflow: hidden; /* Hide overflow to prevent scrolling */
        }
        
        header {
            background-color: #333;
            color: #fff;
            padding: 20px 0;
            position: relative;
            z-index: 1; /* Ensure header is above the live wallpaper */
        }

        .container {
            width: 80%;
            margin: 0 auto;
        }
        
        h1 {
            margin: 0;
            font-size: 24px;
            font-weight: normal;
        }
        
        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }
        
        nav ul li {
            display: inline;
            margin-right: 20px;
        }
        
        nav ul li a {
            color: #fff;
            text-decoration: none;
            padding: 5px 10px;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        
        nav ul li a:hover {
            background-color: #555;
        }
        
        .hero {
            background-image: url('https://cdn.pixabay.com/photo/2021/06/04/05/45/god-6308823_1280.jpg');
            background-size: cover; /* Cover the entire viewport */
            background-position: center;
            color: #fff;
            text-align: center;
            padding: 100px 0;
            width: auto;
            position: relative;
            z-index: 0; /* Ensure hero is behind the live wallpaper */
        }

        .live-wallpaper {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1; /* Ensure live wallpaper is behind everything */
            pointer-events: none; /* Make wallpaper non-interactive */
            animation: liveWallpaper 20s linear infinite; /* Animation for live wallpaper */
        }

        @keyframes liveWallpaper {
            0% {
                background-color: rgba(0, 0, 0, 0.5);
            }
            25% {
                background-color: rgba(255, 255, 255, 0.5);
            }
            50% {
                background-color: rgba(0, 0, 0, 0.5);
            }
            75% {
                background-color: rgba(255, 255, 255, 0.5);
            }
            100% {
                background-color: rgba(0, 0, 0, 0.5);
            }
        }

        .featured-destinations {
            background-color: #f4f4f4;
            padding: 50px 0;
            text-align: center;
        }
        
        .destination {
            display: inline-block;
            margin: 20px;
            vertical-align: top;
            width: calc(33.33% - 40px); /* Adjust as needed */
        }
        
        .destination img {
            max-width: 100%;
            border-radius: 5px;
        }
        
        .destination h3 {
            margin: 10px 0;
        }
        
        .destination a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #333;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        
        .destination a:hover {
            background-color: #555;
        }

        .user-info {
            position: absolute;
            top: 20px;
            right: 20px;
            color: #fff;
            font-weight: bold;
        }

        .signout {
            background-color: #f00;
            border: none;
            color: #fff;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <h1>Travel Website</h1>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                    <li><a href="/destination">Destinations</a></li>
                    <li><a href="/about">About</a></li>
                    <li><a href="/contact">Contact</a></li>
                    <!-- This part is replaced dynamically -->
                    <% if (username) { %>
                        <div class="user-info">
                            Welcome, <%= username %>!
                            <form action="/logout" method="get"> <!-- Changed action to /logout -->
                                <button type="submit" class="signout">Sign Out</button>
                            </form>
                        </div>
                    <% } else { %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/signup">Signup</a></li>
                    <% } %>
                </ul>
            </nav>
        </div>
    </header>

    <section class="hero">
        <div class="live-wallpaper"></div> <!-- Live wallpaper -->
        <div class="container">
            <h2>Explore the World</h2>
            <p>Find your next adventure</p>
        </div>
    </section>

    <section class="featured-destinations">
        <div class="container">
            <h2>Featured Destinations</h2>
            <!-- Add your destination content here -->
        </div>
    </section>

    <!-- Your existing footer content -->

</body>
</html>
